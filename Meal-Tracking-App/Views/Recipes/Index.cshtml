@model List<Meal_Tracking_App.Models.Recipe>

<h1>Favorite Foods, Meals, and Recipes</h1>

<p>
    <a asp-controller="Recipes" asp-action="Add">Add Recipe</a>
</p>


<p>
    <a asp-controller="Recipes" asp-action="Delete">Delete Recipe</a>
</p>

@if (Model.Count == 0)
{
    <p>No recipes yet!</p>
}
else
{
    @*<table class="table">
        <tr>
            <th>
                Id
            </th>
            <th>
                Name
            </th>
        </tr>
        @foreach (var recipe in Model)
        {
            <tr>
                <td>@recipe.Id</td>
                <td><a asp-controller="Recipes" asp-action="Detail" asp-route-id="@recipe.Id">@recipe.Name</a></td>
            </tr>
        }
    </table>*@
    <ul>
        @foreach (var recipe in Model)
        {
            <li><a asp-controller="Recipes" asp-action="Detail" asp-route-id="@recipe.Id">@recipe.Name</a></li>
        }
    </ul>
}
